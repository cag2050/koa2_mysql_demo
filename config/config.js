const config = {
    port: 3001,
    database: {
        DATABASE: 'member',
        USERNAME: 'root',
        PASSWORD: 'mysql',
        PORT: '3306',
        HOST: 'localhost'
    }
}

module.exports = config